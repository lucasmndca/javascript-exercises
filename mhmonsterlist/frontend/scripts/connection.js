//As browser do not natively support modules, then I'll invoke an object right here.
function openConnection() {
    let service = new Service();
    service.initialDatabaseFetch();
    console.log("MIQUINHA EU TE AMO MEU TUDO")
}

